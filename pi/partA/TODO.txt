Add a break statement inside the bug sweep function.
From here print out all the values (bruteforce style)
THen, use awk to find where the functions are being called that whose supports are one off
That way we can check to make sure it is incremented
